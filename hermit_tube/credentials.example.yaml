api_key: my_youtube_api_key
salt: bunch'o'random
wsgi:
  GOOGLE_CLIENT_ID: supersecret.apps.googleusercontent.com
  GOOGLE_CLIENT_SECRET: superDuperSecret
  SECRET_KEY: "my-safe-cookies-need-randomness"
  # The root domain (foo.com)
  CORS_ORIGINS: "http://localhost:8000"
  # The wsgi domain, designed to be a subdomain (auth.foo.com)
  DOMAIN: localhost:5000
bucket:
  name: my-bucket
  # s3 or dreamobject server
  url: https://foo.io
  access_key: short_secret
  secret: long_secret